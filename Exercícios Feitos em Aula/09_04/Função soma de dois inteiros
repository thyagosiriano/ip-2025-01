package main

import "fmt"

func main() {
	n1, n2 := 0, 0
	fmt.Scan(&n1)
	fmt.Scan(&n2)

	s := soma(n1, n2)
	fmt.Printf("A soma entre %d e %d é %d", n1, n2, s)
}

func soma(numero1, numero2 int) int {
	return numero1 + numero2
}
