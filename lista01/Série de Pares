programa {
  funcao inicio() {
  inteiro n1, n2, contador

    contador = 0

    escreva("Insira aqui o primeiro número: ")
    leia (n1)
    escreva("Insira aqui o segundo número: ")
    leia(n2)

    se (n1 % 2 == 0){
      enquanto (contador != n2){
        contador = contador + 1
        escreva(" ", n1)
        n1 = n1 + 2
      }
    }
    senao{
      escreva("O PRIMEIRO NÚMERO NÃO É PAR")
    }
  }
}
